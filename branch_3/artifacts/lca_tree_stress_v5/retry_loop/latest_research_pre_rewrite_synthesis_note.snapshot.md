# Pre-Rewrite Synthesis Note

Date: `2026-04-12`

This is the short branch-local pre-rewrite checkpoint summary to cite before any
major solver rewrite or pivot in `branch_3`. It distills the current
branch-local research notes plus the bundled `progress40`
source/report/results set and does not replace the fuller ledger in
`boj28350_resume/pre_rewrite_checkpoint.md`.

## 2026-04-12 pre-rewrite checkpoint note

This is the current `branch_3` research-log checkpoint to cite before any
major solver rewrite or pivot is opened. It refreshes the paired-review record
so the latest working-tree note and the standalone checkpoint file stay aligned
on the same date.

- `reviewed source set A`: COMPLETE on `2026-04-12` after re-reading the
  branch-local notes / proof / active-solver package that still defines the
  allowed solver family and the current branch-local drift
- `reviewed source set B`: COMPLETE on `2026-04-12` after re-reading the
  bundled `progress40` source / report / results package that still defines the
  intended optimization corridor and residual ordering

### 2026-04-12 paired completed-review checkpoint

Before any major solver rewrite or research pivot begins in `branch_3`, this
checkpoint explicitly references both completed reviews together:

1. `completed review A`: the branch-local `branch_3` notes / proof /
   active-solver review for `reviewed source set A`, recorded as COMPLETE in
   this note and in `boj28350_resume/pre_rewrite_checkpoint.md`
2. `completed review B`: the bundled `progress40` authoritative source /
   report / results review for `reviewed source set B`, recorded as COMPLETE in
   this note and in `boj28350_resume/pre_rewrite_checkpoint.md`
3. `rewrite/pivot precondition`: no major solver rewrite or research pivot may
   begin unless both completed reviews above are cited together first

This paired-review checkpoint is the explicit pre-rewrite entry for the current
workspace. It keeps the review ordering on record in the same working tree
before any later planning note, retry note, or solver-side strategy pivot tries
to claim rewrite permission.

### 2026-04-12 decision-order confirmation

This entry is the current research-log record that the rewrite/pivot decision
for `branch_3` was taken only after both required source sets were reviewed in
the same workspace.

1. `reviewed source set A` was re-confirmed first from the branch-local notes /
   proof / active-solver package.
2. `reviewed source set B` was re-confirmed next from the bundled
   `progress40` source / report / results package.
3. Only after both review completions were on record did this note lock the
   current rewrite/pivot stance: hold broad rewrites, keep the solver inside
   the progress40-derived family, and allow only re-anchoring moves that
   preserve the literature-grade structure.

Joint takeaway locked on `2026-04-12`:

1. Source set A still constrains structure first: the next rewrite must keep
   the literature-grade BC-tree flavored explicit child lattice and the
   `ensureLatticeChildren(...)`, `closeByBCPath(...)`, and
   `buildClosedHandleFromWitness(...)` line, while treating the current
   separator-decomposition drift as something to reduce rather than extend.
2. Source set B still constrains optimization direction first: `progress40` is
   not permission for a new algorithm family, but the cumulative
   pack/normalize -> same-layout reuse -> layout-signature gate ->
   zero-span eligibility -> fastpath commit line, with
   `zero-span eligibility and fastpath commit` still the primary residual and
   `signature source load and materialize` plus
   `layout signature compare and reuse gate core` still secondary.
3. Taken together, both source sets block a broad rewrite/pivot. The next
   solver rewrite may only re-anchor
   `boj28350_resume/boj28350_branch_3_solver.cpp` closer to the bundled
   `progress40` line while preserving the literature-grade structure above; it
   must not widen branch-local drift, open a separator-decomposition family
   detour, or replace the active line with a heuristic-only shortcut.
4. Both source sets also keep reproducibility in scope: bundled `progress40`
   remains `partial`, so no rewrite plan may treat bundled rows as branch-local
   gate closure. Fresh same-worktree `./lca_strong_gate.sh` and
   `./lca_boj3s_gate.sh` evidence still remains mandatory after solver work,
   with no manual artifact cleanup between reruns.

## 2026-04-11 pre-rewrite checkpoint note
This is the earlier `branch_3` research-log checkpoint to cite before any major
solver rewrite or pivot is opened.

- `reviewed source set A`: COMPLETE on `2026-04-11` after re-reading the
  branch-local notes / proof / active-solver package that still defines the
  allowed solver family and the current branch-local drift
- `reviewed source set B`: COMPLETE on `2026-04-11` after re-reading the
  bundled `progress40` source / report / results package that still defines the
  intended optimization corridor and residual ordering

### 2026-04-11 paired completed-review checkpoint

Before any major solver rewrite or research pivot begins in `branch_3`, this
checkpoint now explicitly references both completed reviews together:

1. `completed review A`: the branch-local `branch_3` notes / proof /
   active-solver review for `reviewed source set A`, recorded as COMPLETE in
   this note and in `boj28350_resume/pre_rewrite_checkpoint.md`
2. `completed review B`: the bundled `progress40` authoritative source /
   report / results review for `reviewed source set B`, recorded as COMPLETE in
   this note and in `boj28350_resume/pre_rewrite_checkpoint.md`
3. `rewrite/pivot precondition`: no major solver rewrite or research pivot may
   begin unless both completed reviews above are cited together first

This paired-review checkpoint is the explicit pre-rewrite entry for the current
workspace. It is intentionally redundant so a later planning or retry note can
confirm, without reconstructing the wider history, that both completed reviews
were already on record before broad solver changes were reopened.

### 2026-04-11 decision-order confirmation

This entry is the current research-log record that the rewrite/pivot decision
for `branch_3` was taken only after both required source sets were reviewed in
the same workspace.

1. `reviewed source set A` was re-confirmed first from the branch-local notes /
   proof / active-solver package.
2. `reviewed source set B` was re-confirmed next from the bundled
   `progress40` source / report / results package.
3. Only after both review completions were on record did this note lock the
   current rewrite/pivot stance: hold broad rewrites, keep the solver inside
   the progress40-derived family, and allow only re-anchoring moves that
   preserve the literature-grade structure.

Joint takeaway locked on `2026-04-11`:

1. Source set A still constrains structure first: the next rewrite must keep
   the literature-grade BC-tree flavored explicit child lattice and the
   `ensureLatticeChildren(...)`, `closeByBCPath(...)`, and
   `buildClosedHandleFromWitness(...)` line, while treating the current
   separator-decomposition drift as something to reduce rather than extend.
2. Source set B still constrains optimization direction first: `progress40` is
   not permission for a new algorithm family, but the cumulative
   pack/normalize -> same-layout reuse -> layout-signature gate ->
   zero-span eligibility -> fastpath commit line, with
   `zero-span eligibility and fastpath commit` still the primary residual and
   `signature source load and materialize` plus
   `layout signature compare and reuse gate core` still secondary.
3. Taken together, both source sets block a broad rewrite/pivot. The next
   solver rewrite may only re-anchor
   `boj28350_resume/boj28350_branch_3_solver.cpp` closer to the bundled
   `progress40` line while preserving the literature-grade structure above; it
   must not widen branch-local drift, open a separator-decomposition family
   detour, or replace the active line with a heuristic-only shortcut.
4. Both source sets also keep reproducibility in scope: bundled `progress40`
   remains `partial`, so no rewrite plan may treat bundled rows as branch-local
   gate closure. Fresh same-worktree `./lca_strong_gate.sh` and
   `./lca_boj3s_gate.sh` evidence still remains mandatory after solver work,
   with no manual artifact cleanup between reruns.

### 2026-04-11 active hypotheses, constraints, and rejected directions

This subsection is the live branch-local pre-rewrite note section for the
current `branch_3` workspace. It reconciles the solver-family notes with the
latest retry-loop state before any later rewrite or pivot is opened.

#### Current hypotheses

1. The solver-family hypothesis is still the bundled `progress40` corridor:
   re-anchor `boj28350_resume/boj28350_branch_3_solver.cpp` toward the
   cumulative pack/normalize -> same-layout reuse -> layout-signature gate ->
   zero-span eligibility -> fastpath commit line, not toward a new algorithm
   family.
2. Inside that solver corridor, the safest solver-side primary residual remains
   `zero-span eligibility and fastpath commit`, with
   `signature source load and materialize` plus
   `layout signature compare and reuse gate core` still secondary follow-up
   axes.
3. The live retry-loop blocker is narrower than a solver-family pivot:
   attempt `029` failed on `AC 2` in `./lca_smoke.sh`, with
   `pinned_primary_axis = retain_compaction`,
   `pinned_secondary_axis = state_materialization`, and
   `next_probe_command = ./lca_smoke.sh`. Until that pre-gate stability issue
   is cleared, the smoke wrapper failure should be treated as a retry-entry
   blocker, not as permission to broaden the solver rewrite away from the
   progress40 baseline.
4. The active solver still carries the route-aware `time_lgate_*` /
   `lgate_*` surfaces, so later optimization work should preserve those
   counters and narrow cost inside them rather than deleting the instrumentation
   that the bundled reports depend on.

#### Current constraints

1. Preserve the literature-grade structure first: keep the BC-tree flavored
   explicit child lattice plus the
   `ensureLatticeChildren(...)` / `closeByBCPath(...)` /
   `buildClosedHandleFromWitness(...)` line. A rewrite that breaks those
   invariants is out of scope for `branch_3`.
2. Treat the current separator-decomposition drift as debt to reduce, not as a
   direction to extend. The next major solver move may re-anchor to bundled
   `progress40`, but it must not widen the branch-local detour.
3. Keep branch-local reproducibility rules active. Bundled `progress40`
   remains `partial`; dense `1024` release/repeat, dense `4096`, multi `4096`,
   and long-run terminal-row persistence still need fresh same-worktree
   branch-local evidence. Formal closure still depends on
   `./lca_strong_gate.sh` and `./lca_boj3s_gate.sh`, not on carried-forward
   bundle rows or stale artifacts.
4. Do not trust git-backed inspection as the primary branch-state oracle right
   now. `latest_git_repo_health.md` still reports timed-out `git` checks, so
   retry planning should rely on the branch-local notes and current retry-loop
   artifacts first.
5. Do not over-read blocked AC3/AC5 states as progress. `latest_attempt_guard.md`
   still rejects nominal closure because direct same-attempt strong/boj3s gate
   evidence is missing.

#### Prior failed directions to keep rejected

1. `shared backbone` stays rejected; hard families did not produce meaningful
   cluster sharing.
2. `owner-local exact oracle` stays rejected; owner-local exact BFS increased
   edge work instead of reducing it.
3. `BC local-surgery` stays rejected; locality gains did not survive the hard
   families.
4. A separator-decomposition expansion or heuristic-only branch-local rewrite
   remains rejected as a `progress40` continuation. The next rewrite must
   narrow drift back toward bundled `progress40`, not rationalize the detour.
5. `connector hotpath normalize reuse` is still not the first performance
   target; the bundled aggregate leaves it at `0.0036%`, far below the
   zero-span and signature/layout residuals.

## 2026-04-10 Sub-AC 3 pre-rewrite checkpoint refresh

This is the current active pre-rewrite checkpoint entry to cite before any
major solver rewrite or pivot begins in `branch_3`.

- `reviewed source set A`: COMPLETE on `2026-04-10` after re-reading the
  branch-local `branch_3` notes / working-set review package:
  `boj28350_resume/README.md`,
  `boj28350_resume/current_state_summary.md`,
  `boj28350_resume/next_session_briefing.md`,
  `boj28350_resume/pre_rewrite_checkpoint.md`,
  `boj28350_resume/progress40_derived_reference.md`,
  `boj28350_complete_master_document_partA_raw.md`,
  `boj28350_integrated_technical_history.md`,
  `boj28350_literature_progress7_bcdecomp_report.md`,
  `literature_grade_proof_package.md`, and
  `boj28350_resume/boj28350_branch_3_solver.cpp`
- `reviewed source set B`: COMPLETE on `2026-04-10` after re-reading the
  bundled `progress40` authoritative materials review set:
  `boj28350_bundle_archive/boj28350_literature_progress40_layout_signature_reuse_gate.cpp`,
  `boj28350_bundle_archive/boj28350_progress40_layout_signature_reuse_gate_report.md`,
  and `boj28350_bundle_archive/boj28350_progress40_results_merged.json`
- `rewrite/pivot start condition`: do not begin any major solver rewrite or
  pivot until a planning or retry note explicitly cites this `2026-04-10`
  checkpoint together with `boj28350_resume/pre_rewrite_checkpoint.md` and
  restates that both review completions remain in force

This `2026-04-10` entry is the explicit paired-review gate for the next major
solver rewrite or pivot: both the `branch_3` notes review and the bundled
`progress40` materials review were re-confirmed before rewrite permission can
be reopened.

### 2026-04-10 task-log confirmation

This working-note checkpoint is the active Sub-AC 3 record for the current
session.

- `reviewed source set A`: COMPLETE before any major solver rewrite or pivot
  begins
- `reviewed source set B`: COMPLETE before any major solver rewrite or pivot
  begins
- `task-log hold`: any later planning or retry note that opens a major solver
  rewrite or pivot must cite this `2026-04-10` checkpoint together with
  `boj28350_resume/pre_rewrite_checkpoint.md`

## 2026-04-04 Sub-AC 3 pre-rewrite checkpoint

This note is the active brief planning/research-log checkpoint for the current
`branch_3` line.

- `reviewed source set A`: COMPLETE on `2026-04-04`
- `reviewed source set B`: COMPLETE on `2026-04-04`
- `rewrite/pivot start condition`: do not begin any major solver rewrite or
  pivot until a later planning or retry note cites this checkpoint together
  with `boj28350_resume/pre_rewrite_checkpoint.md`

The paired review remains the gate: both source sets were reviewed before any
later solver rewrite or pivot may begin, and the allowed direction stays inside
the literature-grade plus bundled `progress40` research line summarized below.

## 2026-04-04 refresh synthesis

This dated refresh reaffirms the two source sets that must ground any later
major rewrite:

- branch-local notes side: `boj28350_resume/README.md`,
  `boj28350_resume/current_state_summary.md`,
  `boj28350_resume/next_session_briefing.md`,
  `boj28350_complete_master_document_partA_raw.md`,
  `boj28350_integrated_technical_history.md`,
  `boj28350_literature_progress7_bcdecomp_report.md`,
  `literature_grade_proof_package.md`,
  `boj28350_resume/progress40_derived_reference.md`,
  `boj28350_resume/pre_rewrite_checkpoint.md`
- bundled progress40 side:
  `boj28350_bundle_archive/boj28350_literature_progress40_layout_signature_reuse_gate.cpp`,
  `boj28350_bundle_archive/boj28350_progress40_layout_signature_reuse_gate_report.md`,
  `boj28350_bundle_archive/boj28350_progress40_results_merged.json`

Short synthesis locked again on `2026-04-04`:

1. The branch-local notes still say the active solver started from a bundled
   `progress40` snapshot but has drifted into a separator-decomposition branch.
   Any later major rewrite must reduce that drift by re-anchoring to bundled
   `progress40`, not by extending the branch-local detour.
2. The proof/history package still fixes the allowed structure: preserve the
   literature-grade BC-tree flavored explicit child lattice and the
   `ensureLatticeChildren(...)`, `closeByBCPath(...)`,
   `buildClosedHandleFromWitness(...)` path rather than pivoting to a different
   algorithm family.
3. The bundled `progress40` materials still fix the optimization corridor:
   route-aware `lgate_*` / `time_lgate_*` attribution stays in scope, and the
   first residual axis remains `zero-span eligibility and fastpath commit`,
   with `signature source load and materialize` plus
   `layout signature compare and reuse gate core` remaining secondary.
4. The bundled package is still only `partial`, so it cannot substitute for
   fresh same-worktree branch-local required-gate evidence. Any future rewrite
   plan must keep reproducibility and fresh `./lca_strong_gate.sh` /
   `./lca_boj3s_gate.sh` closure in the contract.

## Source sets summarized

- `reviewed source set A`: branch-local notes and proof/history package:
  `boj28350_resume/README.md`,
  `boj28350_resume/current_state_summary.md`,
  `boj28350_resume/next_session_briefing.md`,
  `boj28350_complete_master_document_partA_raw.md`,
  `boj28350_integrated_technical_history.md`,
  `boj28350_literature_progress7_bcdecomp_report.md`,
  `literature_grade_proof_package.md`,
  `boj28350_resume/progress40_derived_reference.md`,
  `boj28350_resume/pre_rewrite_checkpoint.md`
- `reviewed source set B`: bundled `progress40` materials:
  `boj28350_bundle_archive/boj28350_literature_progress40_layout_signature_reuse_gate.cpp`,
  `boj28350_bundle_archive/boj28350_progress40_layout_signature_reuse_gate_report.md`,
  `boj28350_bundle_archive/boj28350_progress40_results_merged.json`

## Key takeaways from source set A

1. The allowed solver family is still the literature-grade line from the
   progress7/proof-package materials: BC-tree flavored decomposition, explicit
   child lattice semantics, and the minimal closed-subtree handle lift remain
   the structural anchor.
2. In concrete solver terms, a major rewrite must preserve the
   `ensureLatticeChildren(...)`, `closeByBCPath(...)`, and
   `buildClosedHandleFromWitness(...)` path rather than swap in a different
   algorithm family or a heuristic-only branch-local shortcut.
3. The branch-local notes still describe the active solver as having
   separator-decomposition drift relative to the intended line, so the next
   rewrite should reduce that drift, not widen it.
4. Branch-local validation and reproducibility constraints are part of the
   research contract: later closure still requires fresh same-working-tree
   branch-local evidence from `./lca_strong_gate.sh` and
   `./lca_boj3s_gate.sh`, with no manual artifact cleanup between reruns.

## Key takeaways from source set B

1. Bundled `progress40` is still the authoritative optimization direction for
   this branch. It is not a new solver family; it is the accumulated
   pack/normalize -> same-layout reuse -> layout-signature gate ->
   zero-span eligibility -> fastpath commit line on top of `progress39`.
2. The active solver still carries the route-aware `time_lgate_*` / `lgate_*`
   surfaces from that line, so future optimization should narrow cost inside
   this corridor instead of deleting or bypassing it.
3. The bundled report/results keep the residual order unchanged: the safe next
   primary axis is `zero-span eligibility and fastpath commit`, while
   `signature source load and materialize` and
   `layout signature compare and reuse gate core` stay secondary.
4. The bundled package remains `partial`, so its validator-clean rows are
   useful anchor evidence but do not replace fresh branch-local required-gate
   reruns.

## Locked rewrite constraints

1. Stay inside the current research family. A major rewrite or pivot must
   re-anchor `boj28350_resume/boj28350_branch_3_solver.cpp` to the bundled
   `progress40` line while preserving the literature-grade BC-tree / closed
   handle structure from source set A.
2. Preserve the cumulative `progress40` optimization corridor. The next rewrite
   should attack cost inside the existing layout-gate and zero-span fastpath
   corridor, not turn this branch into a different decomposition line.
3. Keep the residual order fixed unless fresh evidence disproves it. The safe
   next axis remains `zero-span eligibility and fastpath commit`; secondary
   axes remain `signature source load and materialize` and
   `layout signature compare and reuse gate core`.
4. Treat reproducibility as part of the rewrite contract. Any later rewrite or
   pivot still needs fresh same-working-tree branch-local evidence from
   `./lca_strong_gate.sh` and `./lca_boj3s_gate.sh`, with no manual artifact
   cleanup between reruns.

## Use

Any future planning or retry note that opens a major solver rewrite should cite
this summary together with `boj28350_resume/pre_rewrite_checkpoint.md` and
restate that both reviewed source sets are complete.
