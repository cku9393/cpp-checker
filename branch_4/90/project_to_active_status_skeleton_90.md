# Project To Active Status Skeleton 90

## status after locality refinement

`proof_ready_skeleton_project_to_active_locality_status_domain_open`

The selected project-to-active status statement is now refined to:

`project_to_active_status_preserved_under_locality_or_reduced_or_escape`.

This does not prove the full operation. It separates the prior
inactive-support locality blocker into:

- payload locality: `inactive_support_payload_locality_proved_under_current_scope`;
- counterexample-status locality:
  `inactive_support_counterexample_status_locality_proof_ready_status_domain_open`;
- preservation branch: proof-sketch once status-domain invariance and normal
  form transfer are supplied;
- reduction branch: proof-sketch using existing strict active-subset measure
  decrease if the projected status is a valid reduced counterexample;
- escape branch: current-scope named project-to-active blocker or deferred
  higher-support escape after operation proofs close.

Runtime skeleton:
`branch_4/90/runtime/project_to_active_status_skeleton_90.tsv`.
