# Family-Chain Absorption Status Skeleton 90

## lemma name

`family_chain_absorption_status_refuted_or_reduced_or_named_escape`.

## exact statement

For a normal support>8 witness in recognized family-chain source form satisfying the family-chain absorption trigger, the absorption branch either:

- refutes the aligned source/target counterexample status by bounded family-chain theorem;
- reduces to a smaller counterexample witness when residual absorption has strict measure decrease and payload/status transfer;
- or routes applicability, payload, status, measure, or normal-form failure to a named operation blocker or deferred higher-support escape.

## assumptions

- family-chain absorption notation and operation semantics are contract-ready;
- the trigger selects a bounded lower-layer target object;
- current lower-layer package is total `7`, fresh `7`, imported `0`;
- self-verification validates current target package identity;
- bounded family-chain theorem applies only to valid aligned target inputs;
- residual branch requires strict support/canonical/depth lexicographic decrease;
- status language separates preservation, reduction, refutation, absorption, and escape.

## conclusion

Family-chain absorption status is first-class and proof-ready as a refutation/reduction/escape skeleton, but source-counterexample refutation, residual measure decrease, and payload/status transfer remain open.

## final status

`proof_ready_skeleton_family_chain_absorption_status_refutation_measure_open`.

Runtime skeleton: `branch_4/90/runtime/family_chain_absorption_status_skeleton_90.tsv`.
