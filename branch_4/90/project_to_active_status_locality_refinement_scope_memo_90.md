# Project To Active Status Locality Refinement Scope Memo 90

## selected target

`project_to_active_status_locality_refinement`

## selected statement

`project_to_active_status_preserved_under_locality_or_reduced_or_escape`

This round combines the current-scope payload locality row, a proof-ready
counterexample-status locality refinement, and the existing smaller-witness
measure guard:

- inactive support is outside relevant payload carriers under the active support
  contract;
- counterexample status is preserved only when status dependencies and the
  status predicate domain are invariant under restriction and normalization;
- if the projected witness changes domain but remains a valid counterexample or
  reduced obstruction, the existing strict active-subset measure decrease gives
  the smaller-witness branch;
- if locality, domain transfer, normal form, or reduced-status validity is not
  established, the branch is named as a project-to-active operation blocker or a
  deferred higher-support escape after operation proofs close.

The full `full_project_to_active_status_locality` statement is out of scope.
This memo does not prove the full project-to-active operation, support-bound
completion, support8 sufficiency, a support9+ scan, or the full general theorem.

Runtime inventory:
`branch_4/90/runtime/project_to_active_status_locality_refinement_scope_inventory_90.tsv`.
