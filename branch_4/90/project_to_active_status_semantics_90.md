# Project To Active Status Semantics 90

## status

`project_to_active_status_semantics_contract_ready`

## summary

The status semantics distinguish preservation, reduction, loss, and escape for active projection. The key proof dependency is inactive-support irrelevance for counterexample status. Payload irrelevance follows from the active support definition under the selected contract, but arbitrary counterexample-status locality remains open.

Runtime table: `branch_4/90/runtime/project_to_active_status_semantics_90.tsv`.
