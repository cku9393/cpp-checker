# Support Reduction Operations 90

## operation inventory

The support reduction attempt defines eight candidate operations. None is promoted as an unrestricted support-reduction theorem. The currently strongest result is an operation/escape interface:

- declared reduction operations are proof-ready only under explicit preconditions;
- `family_chain_absorption_reduction` is blocked by `family_chain_support_lift`;
- irreducible support `>8` cases are routed to `higher_support_escape`.

Runtime table: `branch_4/90/runtime/support_reduction_operations_90.tsv`.
