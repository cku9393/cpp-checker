# Family-Chain Source Form Classification 90

## classification

The recognizer yields five classes. Only the first two can feed the refined lift map. The remaining classes are named escapes or out-of-family-chain cases.

Runtime inventory: `branch_4/90/runtime/family_chain_source_form_classification_90.tsv`.
