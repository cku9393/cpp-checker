# Support >8 Witness Anatomy 90

## purpose

This memo decomposes a support `>8` normal witness into the components needed by `support_growth_partition`.

The current runtime package defines the support8 finite language and the limited bridge theorem. It does not provide an unrestricted support-shrink operation. Therefore the anatomy separates active support, family-chain components, frontier/shell/tail components, and irreducible higher-support escape triggers.

## status

The anatomy is `support_gt8_anatomy_contract_ready`. It is a proof contract, not a completed reduction proof.

Runtime table: `branch_4/90/runtime/support_gt8_witness_anatomy_90.tsv`.
