# General Gap Bridge Dependency Graph 90

## updated dependency focus

The graph now routes the family-chain branch through phase2 payload refinement:

`source_form_recognizer` -> `refined_lift_map` -> `payload_semantics` -> `phase2_relation/correspondence` -> `counterexample_status_semantics` -> `obstruction_preservation_skeleton`.

The remaining dependency is layerwise semantic payload preservation, canonical lift soundness, counterexample-status preservation, and operation-specific smaller-witness construction.

Runtime graph: `branch_4/90/runtime/general_gap_bridge_dependency_graph_90.tsv`.
