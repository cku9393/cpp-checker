# Inactive Support Payload Locality 90

## status

`inactive_support_payload_locality_proved_under_current_scope`

## summary

The payload side of inactive-support locality is closed for the selected active
support contract. The active set includes payload dependency coordinates, so
restricting to the active set cannot delete those payload carriers.

This does not complete counterexample-status locality. Status may depend on
certificate/status-domain components and on whether the projected normal form
remains in the same status predicate domain.

Runtime table:
`branch_4/90/runtime/inactive_support_payload_locality_90.tsv`.
