# branch_2_2 Outer Suite Wrappers

These wrappers run the shared `lca_tree_stress_v5` harness against the
branch-local solver in `../round45_resume/solve`.

Outputs stay under `../artifacts/lca_tree_stress_v5/`.
