# Round 45 Artifact Notes

This tree contains historical Round 45 smoke outputs plus newer
branch-local verification outputs.

Notes:

- historical `summary.json` and `smoke_summary.tsv` files were path-normalized
  after the branch-local move into `branch_2_2/`
- those files were not regenerated from a fresh solver run during this cleanup
- newer verification outputs such as `smoke_localcheck/` and
  `direct_localcheck/` already reflect the current branch-local layout
