phase21 handoff
timestamp=2026-03-20
status=stopped_midway_before_validation

completed_so_far
- Confirmed no active phase21 build or ctest background processes remained at stop time.
- Audited lifecycle gaps in policy baseline / refresh / rerun-plan / bundle flow.
- Removed duplicate phase21 helper/case definitions from tests/raw_engine_cases.cpp earlier in this session; active duplicate scan now only reports run_campaign_case inside disabled code.
- Updated lifecycle runner calls in tests/raw_engine_cases.cpp so run_policy_ci_check_case() and run_policy_nightly_refresh_case() write the current manifest output path after ensure_current_policy_manifest().
- Collected review feedback from reviewer/tester/documenter subagents and consolidated the concrete remaining risks below.

current_blockers
1. selective rerun readback gap
   - execute_policy_rerun_plan() writes applicability/lineage reruns under nightly_reruns/... in tests/raw_engine_cases.cpp
   - build_policy_gate_manifest() only reloads fixed phase17-phase21 locations in tests/policy_gate.cpp
   - result: a real stale rerun can execute but still remain stale on subsequent refresh because the refreshed summaries are not read back into the manifest source-of-truth.

2. baseline promotion provenance bug
   - promote_policy_gate_baseline() in tests/policy_baseline.cpp still calls assign_policy_manifest_input_hashes(promoted, sourceRoot)
   - result: approved baseline can silently recompute provenance from live checkout instead of freezing the hashes actually measured in the source manifest.

3. lifecycle runner omission risk
   - run_policy_ci_check_case() currently sets planOptions.includeDiagnostic = options.includeDiagnostic and includeNonApplicable = options.includeNonApplicable
   - result: stale NON_APPLICABLE / DIAGNOSTIC_ONLY families can be omitted from rerun plan unless caller remembered the flags.

4. compare_ready evidence source mismatch
   - planner_tie_mixed_organic_compare_ready still uses DIAGNOSTIC_LINEAGE + CAMPAIGN_COMPARE as freshness sources in tests/policy_manifest_freshness.cpp and tests/policy_gate.cpp
   - rerun plan only schedules lineage_audit_rerun for diagnostic-only family
   - result: diagnostic-only family cannot reliably return to FRESH from generated rerun flow.

5. baseline acceptance too weak
   - policy_manifest_acceptable_for_baseline() only checks non-empty manifest + acceptable family states
   - result: a filtered or partial manifest could be promoted as approved baseline.

6. history/bundle metadata gaps
   - write_policy_baseline_outputs_with_history() writes bundle_file= empty
   - make_plan_entry() still recommends phase19 applicability/lineage artifact paths
   - build_evidence_bundle.py likely still prefers latest-by-mtime applicability/lineage summaries instead of manifest-linked summaries.

recommended_next_edits
1. tests/policy_baseline.cpp
   - preserve source manifest relevant-input hashes during promote_policy_gate_baseline(); do not recompute live provenance when freezeProvenance is requested.
   - strengthen policy_manifest_acceptable_for_baseline() to require the full expected family set:
     split_tie_organic_symmetric
     automorphism_probe_large
     planner_tie_mixed_organic
     planner_tie_mixed_organic_compare_ready
   - fill bundle_file in write_policy_baseline_outputs_with_history() using promoted report/bundle path resolution.
   - update make_plan_entry() recommended applicability/lineage commands from phase19 paths to phase21/current paths.

2. tests/policy_manifest_freshness.cpp
   - change default_evidence_sources_for_family("planner_tie_mixed_organic_compare_ready") to DIAGNOSTIC_LINEAGE only, unless rerun flow is expanded to refresh compare campaign evidence too.

3. tests/policy_gate.cpp
   - align evaluate_compare_ready_diagnostic_gate() evidenceSources with diagnostic-only policy.
   - consider setting manifest.reportVersion to phase21 instead of default phase19.
   - either teach build_policy_gate_manifest() to read rerun output locations, or make rerun execution write back into the canonical phase21 applicability/lineage locations that this function already reads.

4. tests/raw_engine_cases.cpp
   - make lifecycle runners include DIAGNOSTIC_ONLY and NON_APPLICABLE families by default, or fail when omittedFamilies is non-empty without explicit family filter.
   - validate externally supplied rerun plan against baseline/current/refresh hashes before execution.
   - change execute_policy_rerun_plan() applicability/lineage outputs to canonical phase21 artifact locations, or a location that build_policy_gate_manifest() will consume.

5. README.md
   - document canonical distinction between:
     artifacts/manifests/policy_gate.json
     artifacts/manifests/policy_graduation_manifest_v1.json
     lifecycle summaries such as policy_ci_summary_phase21.txt
   - add baseline history inspection section
   - add selective rerun execution via policy_nightly_refresh
   - add evidence bundle inspection section
   - keep compare_ready wording explicitly diagnostic-only

targeted_validation_after_edits
1. build
   - cmake --build build-release-phase21 -j4 --target raw_engine_tests

2. targeted smokes
   - ctest --test-dir build-release-phase21 -R "policy_(ci_check|nightly_refresh|refresh_stale_hash|refresh_compare_engine_hash|refresh_generator_hash|reclassify_non_applicable|reject_diagnostic_promotion|rerun_plan_mode_selection|selective_rerun_execute|selective_rerun_noop|baseline_history|baseline_archive_roundtrip|gate_manifest|refresh_manifest|evidence_bundle_idempotent|evidence_bundle_policy_summary)_smoke" --output-on-failure

3. explicit policy flows
   - ./build-release-phase21/raw_engine_tests --case policy_ci_check --baseline-manifest artifacts/manifests/policy_graduation_manifest_phase21_approved_v1.json --current-manifest artifacts/manifests/policy_graduation_manifest_v1.json --refresh-manifest artifacts/manifests/policy_graduation_manifest_refresh_phase21_v1.json --rerun-plan artifacts/manifests/policy_rerun_plan_phase21_v1.json --emit-summary artifacts/manifests/policy_ci_summary_phase21.txt --strict-refresh --allow-empty-plan
   - ./build-release-phase21/raw_engine_tests --case policy_nightly_refresh --baseline-manifest artifacts/manifests/policy_graduation_manifest_phase21_approved_v1.json --current-manifest artifacts/manifests/policy_graduation_manifest_v1.json --refresh-manifest artifacts/manifests/policy_graduation_manifest_refresh_phase21_nightly_v1.json --rerun-plan artifacts/manifests/policy_rerun_plan_phase21_nightly_v1.json --emit-summary artifacts/manifests/policy_nightly_refresh_phase21.txt --allow-empty-plan
   - synthetic stale hash injection
   - synthetic applicability drift injection
   - diagnostic promotion rejection
   - baseline promotion/history/archive
   - evidence bundle generation

4. full validation only after targeted smokes pass
   - Release + CTest
   - Debug + CTest
   - ASan/UBSan + CTest isolated clean rerun

file_state_notes
- tests/policy_baseline.cpp currently exists and contains the remaining provenance/history/rerun-plan issues above.
- tests/policy_gate.cpp / tests/policy_manifest_freshness.cpp still reflect the old compare_ready freshness-source choice.
- tests/raw_engine_cases.cpp compiles logically better than before, but still needs lifecycle omission/readback fixes.
- tests/CMakeLists.txt already appears to include most phase21 smoke registrations.
- tests/tools/build_evidence_bundle.py already contains policy_summary/current_verdict/freshness_verdict/family_status_table fields, but needs manifest-linked summary selection review.

subagent_findings_used
- tester: missing readback validation, missing campaign_config_hash smoke, weak real rerun execution coverage, stale recommended paths, bundle/history metadata gaps.
- reviewer: rerun outputs not wired back, live provenance recompute bug, stale family omission risk, rerun plan validation weakness, bundle can package stale artifacts, baseline promotion accepts partial manifest, reportVersion default brittle.
- documenter: README needs canonical manifest distinction, baseline history inspection, policy_nightly_refresh execution path, evidence bundle inspection.

background_process_state_at_stop
- no active build-release-phase21 / build-debug-phase21 / build-asan-phase21 / ctest / raw_engine_tests processes were running at the time of stop check.

final_note
- No src primitive/planner semantics were modified in this interrupted pass.
- No validation run was completed after the latest code-state audit, so phase21 should be treated as in-progress.
