PHASE25 STABILIZATION REPORT
date=2026-03-22
package=raw_engine_v1_package
branch=2_1
authoritative_source=split tree
production_split_choice_policy=exact_shadow
status=phase25_completed_with_runtime_action_required

1. Modified / Added Files
- README.md
- tests/CMakeLists.txt
- tests/raw_engine_cases.cpp
- tests/tools/build_evidence_bundle.py
- tests/tools/run_policy_pipeline.py
- tests/tools/runtime_gate_lib.py

2. Runtime Baseline Registry Multi-Fingerprint Design
- Phase25 keeps the runtime baseline registry as the operator-approved source of runtime comparability and extends validation through multi-fingerprint fixture coverage.
- Active registry artifact:
  `artifacts/manifests/runtime_baseline_registry_v1.json`
- Registry state at phase25 close:
  `entry_count=2`
  `active_entry_count=1`
  `retired_entry_count=1`
  active baseline:
  `phase24-runtime-approved-c89a575d-20260322_112025`
  retired baseline:
  `phase23-runtime-approved-c89a575d-20260322_083433`
- Phase25 fixture matrix validates:
  `exact_current`
  `same_host_compiler_bump`
  `sanitizer_change`
  `runner_tag_change`
  `cross_host`
  `retired_only`
- Fixture outputs remain machine-readable through:
  `selected_baseline_id`
  `selected_baseline_tag`
  `candidate_count`
  `exact_match_count`
  `compatible_match_count`
  `retired_match_count`
  `comparability_reason`

3. Host / Toolchain Selection And Comparability Results
- Correctness lifecycle remains unchanged and healthy:
  `split_tie_organic_symmetric = PASS / FRESH`
  `automorphism_probe_large = PASS / FRESH`
  `planner_tie_mixed_organic = NON_APPLICABLE / FRESH`
  `planner_tie_mixed_organic_compare_ready = DIAGNOSTIC_ONLY / FRESH`
- Phase25 runtime current:
  `artifacts/manifests/policy_runtime_current_phase25.json`
- Phase25 runtime refresh:
  `artifacts/manifests/policy_runtime_refresh_phase25.json`
- Phase25 runtime rerun plan:
  `artifacts/manifests/policy_runtime_rerun_phase25.json`
- Runtime wall times captured into the current manifest:
  `release_full=214.89s`
  `debug_full=818.32s`
  `asan_full=2490.11s`
  `policy_core=0.03s`
  `policy_refresh=0.03s`
  `policy_nightly=0.014s`
- Selected baseline:
  `phase24-runtime-approved-c89a575d-20260322_112025`
  tag:
  `phase24-runtime-approved`
- Runtime refresh verdict:
  `current_verdict=WARN`
  `freshness_verdict=NOT_COMPARABLE`
  `comparability_verdict=NOT_COMPARABLE`
  `overall_budget_verdict=BUDGET_WARN`
  `runtime_severity=ACTION_REQUIRED`
- Comparability reason:
  `same host runtime baseline candidate differs by build or sanitizer signature`
- Interpretation:
  the host fingerprint stayed on the same machine family, but phase25 runtime evidence is only compatible, not strictly comparable, against the approved phase24 runtime baseline.

4. Runtime History / Trend Archive Results
- Runtime history artifacts:
  `artifacts/manifests/runtime_history_index_v1.json`
  `artifacts/manifests/runtime_history_index_v1_summary.json`
- Phase25 nightly appended the new runtime current/refresh into the existing history.
- Single active fingerprint remains:
  `c89a575d84b0fcd2de355c40eeb734cc46bab4e505fc2e1741b67466ec73731a`
- Trend counts after phase25 append:
  `stable=3`
  `regressing=3`
  `noisy=0`
  `improved=0`
  `insufficient_history=0`
- Execution-class summary:
  `release_full sample_count=4 trend=stable latest=214.89s budget=PASS`
  `debug_full sample_count=4 trend=stable latest=818.32s budget=PASS`
  `asan_full sample_count=4 trend=regressing latest=2490.11s budget=BUDGET_WARN`
  `policy_core sample_count=4 trend=regressing latest=0.03s budget=PASS`
  `policy_refresh sample_count=4 trend=regressing latest=0.03s budget=PASS`
  `policy_nightly sample_count=4 trend=stable latest=0.014s budget=PASS`
- The trend archive remains advisory only. It did not alter correctness gate state, but it did reinforce the runtime triage signal that the current environment should not be treated as a strict continuation of the approved phase24 runtime baseline.

5. Runtime Rebaseline Proposal Multi-Candidate Results
- Proposal artifact:
  `artifacts/manifests/runtime_rebaseline_proposal_phase25.json`
- Proposal verdict:
  `proposal_needed=true`
  `comparability_verdict=NOT_COMPARABLE`
  `selected_baseline_id=phase24-runtime-approved-c89a575d-20260322_112025`
  `selected_baseline_tag=phase24-runtime-approved`
  `suggested_new_baseline_tag=phase25-runtime-approved`
- Proposal reason:
  `same host runtime baseline candidate differs by build or sanitizer signature`
- Comparable candidate list retained in the proposal:
  `phase23-runtime-approved` (`retired`)
  `phase24-runtime-approved` (`active`)
- Multi-candidate direct smoke also passed explicitly:
  `runtime_rebaseline_proposal_multi_candidate_smoke`
- Conclusion:
  phase25 does not auto-promote a new runtime baseline. It emits an operator-facing proposal and keeps promotion as a separate explicit approval action.

6. Combined Pipeline Matrix Results
- Combined quick summary:
  `artifacts/manifests/policy_pipeline_quick_phase25.json`
- Combined nightly summary:
  `artifacts/manifests/policy_pipeline_nightly_phase25.json`
- Combined matrix summary:
  `artifacts/manifests/policy_pipeline_matrix_phase25.json`
- Synthetic matrix config used for the final operator run:
  `artifacts/manifests/runtime_matrix_phase25.json`
- Quick result:
  `severity=ACTION_REQUIRED`
  `exit_code=20`
  `runtime_recommendation=NOT_COMPARABLE`
- Nightly result:
  `severity=ACTION_REQUIRED`
  `exit_code=20`
  `runtime_recommendation=NOT_COMPARABLE`
  correctness rerun remained:
  `PASS / empty`
- Final matrix result:
  `severity=ACTION_REQUIRED`
  `exit_code=20`
  `runtime_recommendation=NOT_COMPARABLE`
  `recommended_next_action=runtime baseline is informational only on this host/toolchain; strict comparison is disabled`
- Matrix action distribution:
  `NOT_COMPARABLE=4`
  `REBASELINE_REQUIRED=2`
- Matrix interpretation:
  `exact_current`
  `same_host_compiler_bump`
  `sanitizer_change`
  `runner_tag_change`
  all landed in the non-strict `NOT_COMPARABLE` bucket
  while
  `cross_host`
  `retired_only`
  correctly escalated to `REBASELINE_REQUIRED`
- This confirms the multi-fingerprint selection logic is behaving as intended even though the real phase25 current environment itself is no longer strict-comparable to the approved runtime baseline.

7. Evidence Bundle Runtime Registry / History / Proposal Inclusion
- Final phase25 bundle outputs:
  `artifacts/phase25_evidence_bundle/bundle_metadata.json`
  `raw_engine_phase25_stabilization_20260322.zip`
  `raw_engine_phase25_stabilization_curated_20260322.zip`
  `raw_engine_phase25_stabilization_delivery_20260322.zip`
- Bundle inputs include:
  correctness current / baseline / refresh / rerun
  runtime current / baseline / refresh / rerun
  runtime baseline registry snapshot
  runtime history index and summary
  runtime rebaseline proposal
  combined nightly summary
  combined matrix summary
  selected logs and regression directories
- Bundle metadata is expected to record:
  `runtime_registry_hash`
  `runtime_history_index_hash`
  `runtime_proposal_hash`
  `selected_runtime_baseline_id`
  `selected_runtime_baseline_tag`
  `runtime_trend_summary`
  `runtime_recommendation`
  `rebaseline_proposal_needed`

8. Isolated ASan Full Rerun Result
- Isolated ASan+UBSan full rerun command:
  `ASAN_OPTIONS=detect_leaks=0:abort_on_error=1 UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1 /usr/bin/time -p ctest --test-dir build-asan-phase25 --output-on-failure`
- Result:
  `214/214 PASS`
  `Total Test time (real)=2490.11s`
  `/usr/bin/time real=2490.20s`
- Release full CTest:
  `214/214 PASS`
  `Total Test time (real)=214.89s`
- Debug full CTest:
  `214/214 PASS`
  `Total Test time (real)=818.32s`

9. New Semantic Counterexample
- None.
- No new semantic mismatch was found.
- `src` primitive/planner semantics were not modified.

10. New Regression List
- None.
- No new regression tests were required in phase25.

11. Exact_Shadow Retention Final Verdict
- Exact-shadow correctness remains retained.
- Production correctness evidence remains:
  direct compare `PASS` families:
  `split_tie_organic_symmetric`
  `automorphism_probe_large`
  direct applicability evidence:
  `planner_tie_mixed_organic = NON_APPLICABLE`
  diagnostic-only lineage evidence:
  `planner_tie_mixed_organic_compare_ready = DIAGNOSTIC_ONLY`
- Phase25 changes do not weaken the correctness conclusion.
- The only new operator action is runtime-side:
  refresh is no longer strict-comparable against the approved phase24 runtime baseline,
  so runtime promotion should be considered explicitly before treating phase25 runtime numbers as the new approved baseline.

12. Remaining Risks
- Main operational risk now is runtime baseline maintenance, not correctness.
- The current environment is only `NOT_COMPARABLE` against the active runtime baseline, so strict runtime regression claims should not be made until a new runtime baseline is explicitly approved or the operator reruns on a fully matching execution signature.
- ASan currently lands in `BUDGET_WARN` and `regressing` trend state, which is advisory under a non-comparable refresh but still worth tracking before any runtime baseline promotion.
- The runtime history archive is still single real fingerprint plus synthetic fixtures. It is more mature than phase24, but multi-runner real-world history is still shallow.
- `planner_tie_mixed_organic_compare_ready` remains intentionally diagnostic-only and is still excluded from production correctness evidence.
