PHASE24 STABILIZATION REPORT
date=2026-03-22
package=raw_engine_v1_package
branch=2_1
authoritative_source=split tree
production_split_choice_policy=exact_shadow
status=phase24_completed

1. Modified / Added Files
- README.md
- tests/CMakeLists.txt
- tests/raw_engine_cases.cpp
- tests/raw_engine_main.cpp
- tests/test_harness.hpp
- tests/tools/build_evidence_bundle.py
- tests/tools/run_policy_pipeline.py
- tests/tools/runtime_gate.py
- tests/tools/runtime_gate_lib.py
- tests/runtime_budget_phase24.json

2. Runtime Baseline Registry Design
- Phase24 lifts runtime from a single approved baseline file to a registry keyed by runtime fingerprint.
- Registry artifact:
  `artifacts/manifests/runtime_baseline_registry_v1.json`
- Each registry entry records:
  `baseline_id`
  `baseline_tag`
  `approval_timestamp_utc`
  `runtime_baseline_manifest_path`
  `host_fingerprint`
  `toolchain_fingerprint`
  `build_classes_covered`
  `runtime_budget_profile_id`
  `status`
- Registry activation is explicit:
  the previous active baseline is retired
  the new baseline becomes the single active entry for that fingerprint
- Final phase24 registry state:
  `active_entry_count=1`
  `retired_entry_count=1`
  active baseline id:
  `phase24-runtime-approved-c89a575d-20260322_112025`
  retired baseline id:
  `phase23-runtime-approved-c89a575d-20260322_083433`

3. Host / Toolchain Comparability And Selection Rules
- Runtime baseline selection is now registry-driven and machine-readable.
- Selection output artifact:
  `artifacts/manifests/policy_runtime_current_phase24_baseline_selection.json`
- Selection policy:
  exact fingerprint match => `COMPARABLE`
  compatible candidate without strict execution-class parity => advisory candidate only
  no suitable candidate => `REBASELINE_REQUIRED`
- Runtime fingerprint components remain:
  OS / OS release
  CPU arch
  compiler command
  compiler id / version
  runner tag
  build type / sanitizer flags at execution-class level
- Final phase24 selection result:
  `selected_baseline_tag=phase24-runtime-approved`
  `selected_baseline_id=phase24-runtime-approved-c89a575d-20260322_112025`
  `comparability_verdict=COMPARABLE`
  `candidate_count=2`
  reason:
  `active exact runtime fingerprint match`

4. Runtime History / Trend Archive
- Runtime history artifacts:
  `artifacts/manifests/runtime_history_index_v1.json`
  `artifacts/manifests/runtime_history_index_v1_summary.json`
- Phase24 appends:
  phase23 runtime current/refresh
  phase24 runtime current/refresh
- History remains advisory only. It does not alter correctness gate results.
- Current registry fingerprint count:
  `1`
- Current execution-class trend coverage:
  `release_full`
  `debug_full`
  `asan_full`
  `policy_core`
  `policy_refresh`
  `policy_nightly`
- Per-class sample count is now `3` across the single active fingerprint and all classes are currently reported as:
  `trend_direction=stable`
- Notable advisory deltas in the history summary:
  `release_full rolling_median=208.97s p90=247.22s latest=208.97s`
  `debug_full rolling_median=747.08s p90=789.76s latest=747.08s`
  `asan_full rolling_median=825.03s p90=2267.78s latest=825.03s`
- The ASan history remains advisory only because the phase24 runtime baseline was intentionally rebased after the registry detected a workload/baseline mismatch.

5. Runtime Rebaseline Proposal Workflow
- Proposal artifacts:
  final:
  `artifacts/manifests/runtime_rebaseline_proposal_phase24.json`
  pre-rebaseline evidence snapshot:
  `artifacts/manifests/runtime_rebaseline_proposal_phase24_pre_rebaseline.json`
- Observed workflow:
  1. Phase23 runtime baseline was promoted into the new registry.
  2. Phase24 runtime current was refreshed against that selected baseline.
  3. That first refresh produced:
     `comparability_verdict=REBASELINE_REQUIRED`
     because the old runtime baseline did not fully cover the phase24 workload/operator execution-class shape.
  4. A proposal was emitted with:
     `proposal_needed=true`
     and a suggested registry-promotion command.
  5. Phase24 runtime current was promoted to:
     `policy_runtime_baseline_phase24_approved.json`
  6. The registry was re-activated to make the phase24 baseline current.
  7. Refresh/proposal were rerun and converged to:
     `proposal_needed=false`
     `comparability_verdict=COMPARABLE`
- This keeps runtime rebaseline explicit and operator-approved while allowing the final phase24 runtime lifecycle to return to a healthy state.

6. Runtime Baseline / Current / Refresh / Rerun Results
- Runtime current manifest:
  `artifacts/manifests/policy_runtime_current_phase24.json`
- Runtime approved baseline:
  `artifacts/manifests/policy_runtime_baseline_phase24_approved.json`
- Runtime refresh:
  `artifacts/manifests/policy_runtime_refresh_phase24_v2.json`
- Runtime rerun plan:
  `artifacts/manifests/policy_runtime_rerun_phase24_v2.json`
- Runtime current wall times:
  `release_full=208.97s`
  `debug_full=747.08s`
  `asan_full=825.03s`
  `policy_core=0.023s`
  `policy_refresh=0.023s`
  `policy_nightly=0.014s`
- Final runtime refresh verdict:
  `current_verdict=PASS`
  `freshness_verdict=FRESH`
  `comparability_verdict=COMPARABLE`
  `overall_budget_verdict=PASS`
  `runtime_severity=OK`
  `selected_baseline_tag=phase24-runtime-approved`
- Final runtime rerun plan verdict:
  `summary_verdict=PASS`
  `selected_entry_count=0`
  `stale_entry_count=0`
  `requires_rerun_entry_count=0`
  `rebaseline_required_count=0`

7. Combined Pipeline Operation
- Combined operator summaries:
  `artifacts/manifests/policy_pipeline_quick_phase24.json`
  `artifacts/manifests/policy_pipeline_nightly_phase24.json`
- The pipeline now operates over:
  correctness baseline/current/refresh/rerun
  runtime baseline/current/refresh/rerun
  runtime registry
  runtime history
  runtime proposal
  bundle generation
- Final phase24 quick pipeline:
  `severity=OK`
  `exit_code=0`
  `current_verdict=PASS`
  `freshness_verdict=FRESH`
  `runtime_current_verdict=PASS`
  `runtime_freshness_verdict=FRESH`
  `runtime_comparability_verdict=COMPARABLE`
  `runtime_budget_verdict=PASS`
  `runtime_selected_baseline_id=phase24-runtime-approved-c89a575d-20260322_112025`
  `runtime_rebaseline_proposal_needed=0`
- Final phase24 nightly pipeline:
  `severity=OK`
  `exit_code=0`
  `current_verdict=PASS`
  `freshness_verdict=FRESH`
  `runtime_current_verdict=PASS`
  `runtime_freshness_verdict=FRESH`
  `runtime_comparability_verdict=COMPARABLE`
  `runtime_budget_verdict=PASS`
  `runtime_selected_baseline_id=phase24-runtime-approved-c89a575d-20260322_112025`
  `runtime_rebaseline_proposal_needed=0`
- Recommended next action remains:
  `no action required; exact_shadow lifecycle remains healthy`

8. Correctness Lifecycle Revalidation
- Correctness baseline remains:
  `artifacts/manifests/policy_graduation_manifest_phase22_approved_v1.json`
- Phase24 refresh / rerun artifacts:
  `artifacts/manifests/policy_graduation_manifest_refresh_phase24_v1.json`
  `artifacts/manifests/policy_rerun_plan_phase24_v1.json`
- Correctness family state remains stable:
  `split_tie_organic_symmetric = PASS / FRESH`
  `automorphism_probe_large = PASS / FRESH`
  `planner_tie_mixed_organic = NON_APPLICABLE / FRESH`
  `planner_tie_mixed_organic_compare_ready = DIAGNOSTIC_ONLY / FRESH`
- Production evidence policy remains unchanged:
  `planner_tie_mixed_organic_compare_ready` stays `DIAGNOSTIC_ONLY`
  `counts_as_production_evidence=false`
- No correctness rerun was required in phase24 because the policy lifecycle stayed fresh and rerun plans remained empty.

9. Evidence Bundle Runtime Inclusion
- Bundle outputs:
  `artifacts/phase24_evidence_bundle/bundle_metadata.json`
  `raw_engine_phase24_stabilization_20260322.zip`
  `raw_engine_phase24_stabilization_curated_20260322.zip`
  `raw_engine_phase24_stabilization_delivery_20260322.zip`
- The final bundle now includes:
  correctness manifests
  runtime current/baseline/refresh/rerun
  runtime baseline registry snapshot
  runtime history summary
  runtime proposal
  combined quick/nightly summaries
  the phase24 report
- Bundle metadata records:
  runtime registry hash
  runtime history index hash
  runtime proposal hash
  runtime selected baseline id
  runtime budget summary
  delivery zip metadata

10. Full Validation
- Release full CTest:
  `192/192 PASS`
  `208.97s`
- Debug full CTest:
  `192/192 PASS`
  `747.08s`
- Isolated ASan/UBSan full CTest:
  `192/192 PASS`
  `825.03s`
  command environment:
  `ASAN_OPTIONS=detect_leaks=0:abort_on_error=1`
  `UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1`
- Additional explicit phase24 execution completed:
  runtime registry promote
  runtime registry select
  runtime refresh against selected baseline
  runtime history append
  runtime rebaseline proposal
  combined policy pipeline quick
  combined policy pipeline nightly
  evidence bundle generation

11. Semantic Counterexamples / Regressions
- New semantic counterexample:
  none
- Reproduction command:
  none
- New regression added:
  none

12. Exact Shadow Final Verdict
- Final policy decision:
  `exact_shadow` remains the production split-choice default.
- Supporting basis remains split by evidence type:
  direct production compare evidence:
  `split_tie_organic_symmetric = PASS / FRESH`
  `automorphism_probe_large = PASS / FRESH`
  applicability evidence:
  `planner_tie_mixed_organic = NON_APPLICABLE / FRESH`
  diagnostic lineage evidence:
  `planner_tie_mixed_organic_compare_ready = DIAGNOSTIC_ONLY / FRESH`
- Phase24 changes were strictly operational:
  runtime registry
  runtime history
  runtime proposal
  combined operator pipeline
  evidence bundle expansion
- `src` primitive/planner semantics were not modified.

13. Remaining Risks
- Runtime registry is still single-fingerprint in practice:
  one active host/toolchain entry
  one retired predecessor entry for the same fingerprint
- Cross-host or cross-toolchain runs will still require new runtime baseline approval and will legitimately surface:
  `REBASELINE_REQUIRED`
  or `NOT_COMPARABLE`
- Runtime trend history is still short:
  sample_count=3
  so long-horizon drift interpretation remains advisory, not predictive.
- Correctness remains healthy, but any future semantic mismatch, correctness gate fail, or real planner drift should still trigger:
  dump
  reduce
  regression
  and only then possible `raw_planner.cpp` / primitive changes.
